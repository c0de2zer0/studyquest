name: Deploy to Railway
on:
  push:
    branches: [production-merge]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: railway/action@v3
        with:
          railway_token: ${{ secrets.RAILWAY_TOKEN }}
          service: studyquest
